spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/dosw_library}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:Kironcha25}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true